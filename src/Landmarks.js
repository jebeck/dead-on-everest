import React from 'react';

import './Landmarks.css';

const Landmarks = (props) => (
  <div className="Text">
    <p>
      In general, those who die on Everest <span className="Landmarks-stay">stay on Everest</span>.
      <br/>
      <br/>
      Some corpses even serve as <span className="Landmarks-landmarks">landmarks</span>.
    </p>
  </div>
);

export default Landmarks;
